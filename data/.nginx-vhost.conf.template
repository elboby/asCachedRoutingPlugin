##
## generated by asCachedRoutingPlugin 
## ###%DATETIME%###
## from: ###%TEMPLATEPATH%### 
## saved as: ###%FILEPATH%###
##
## This is the template used to generate the real .htaccess
## You can edit it but you need to leave the rules token in it
##

if ($request_filename !~ "\.(js|htc|ico|gif|jpg|png|css)$") {
  
   ###%RULES_TOKEN%###
  
  rewrite ^(.*) /index.php last;
}
